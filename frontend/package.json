{
  "name": "frontend",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "concurrently \"npm run backend\" \"astro dev\" --names \"backend,frontend\" --prefix-colors \"cyan,magenta\"",
    "dev:frontend-only": "astro dev",
    "backend": "cd .. && uv run run_server.py",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "check": "astro check",
    "start": "npm run dev"
  },
  "dependencies": {
    "astro": "^5.12.3"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "concurrently": "^9.2.1",
    "prettier": "^3.6.2",
    "typescript": "^5.6.0"
  }
}
