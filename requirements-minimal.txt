# Core Trading Backend Dependencies (Minimal)
flask==3.0.3
flask-cors==4.0.0  
flask-limiter==3.5.0
ibind==0.1.18
pycryptodome==3.19.1

# Note: ibind automatically brings in:
# - requests (for HTTP calls) 
# - websocket-client (for real-time data)
# - pycryptodome is required for OAuth crypto operations 